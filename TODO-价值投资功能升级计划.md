# TODO：价值投资功能升级计划（交接文档）

> 写给下一个接手的 AI/开发者。本文档沉淀了 2026-07-24 与用户（华，价值投资者）确认的升级方案、项目现状、关键代码位置和数据纪律。**读完本文档再动手。**

---

## 一、背景与已确认决策

用户是价值投资者，框架为「财报为锚 / 护城河为尺 / 估值为盾 / 管理层为镜」，严守 **ROE 15% 生死线**。

讨论过十几个候选功能后，用户确认**克制原则：只做最小集，不堆功能**。本轮确定做 **4 样**：

| # | 功能 | 优先级 | 状态 |
|---|---|---|---|
| ① | 接行情源（现价）+ 安全边际尺 | 🥇 必做 | 未开始（行情接口已验证思路） |
| ② | 最新股息率（DPS ÷ 现价，实时联动） | 🥇 必做 | 未开始（**DPS 数据待权威核实**） |
| ③ | 买入检查清单（每只一份打勾） | 🥈 | 未开始 |
| ④ | 七力评分卡（7 只持仓护城河） | 🥉 | 未开始 |

**明确不做**（记入 backlog，用户说等真需要再加）：估值温度计、投资决策日志、回购/增减持监测、北极星指标卡、纪要库链接、芒格四分类看板。

**布局约束**（用户已确认）：
- 不新增 tab，整站保持 6 个视图
- ①②③④ 全部塞进现有「📊 总览」页的公司卡里（价格带/检查清单可折叠）
- 首页 KPI 条只加一个「**击球区 N 只**」数字卡（现价较目标价折价 ≥30% 的家数），点击跳总览

---

## 二、项目架构速览

- **本地仓库**：`/Users/huanghua/000-我的GitHub/portfolio-news/`
- **线上**：https://huanghua-2019.github.io/portfolio-news/ （GitHub Pages，push 后 1~2 分钟重建）
- **数据流**：GitHub Actions（`.github/workflows/crawl.yml`，每 30 分钟 cron）→ 跑 `crawler/crawl.mjs`（零依赖 Node）→ 提交 `data/news.json` → 前端纯静态读取（绕开 CORS）
- **前端**：`index.html` + `app.js` + `styles.css`，无框架无构建，缓存版本号当前 **v10**（改了 js/css 记得升 v11）

### 关键文件与代码位置（截至本文档撰写时）

| 位置 | 内容 |
|---|---|
| `holdings.json` | 7 只持仓配置，**每条一行的紧凑风格（勿破坏）**，`_v:3`。已有字段：moat / thesis / targetPrice / financials(5年营收净利ROE) / keyVars / earningsMonths / nextEarningsDate / nextEarningsSource / note |
| `crawler/crawl.mjs` | `nextEarnings(h)` 优先读 holdings 的权威披露日；`companies` 组装处在文件后段（约 228~258 行） |
| `app.js` | `state.holdings` 来自 `news.json.companies`；`renderDash`（总览）约 228 行起；`renderHome`（首页）约 487 行起；`SEC_COLORS` 板块固定色映射（白酒金/互联网蓝/电商红/潮玩紫/航运绿）；`gotoView(view, company)` 跳转辅助；主题切换逻辑在 init 段 |
| `styles.css` | 末尾两大段追加样式：「首页仪表盘」和「全站 UI 升级 v10」；深色主题变量在 `:root` 后的 `[data-theme=dark]` |

### 7 只持仓

| 板块 | 公司 | 代码 | 腾讯行情接口代码 |
|---|---|---|---|
| 白酒 | 贵州茅台 | 600519.SH | `sh600519` |
| 白酒 | 五粮液 | 000858.SZ | `sz000858` |
| 白酒 | 泸州老窖 | 000568.SZ | `sz000568` |
| 互联网 | 腾讯控股 | 00700.HK | `r_hk00700` |
| 电商 | 拼多多 | PDD | `usPDD` |
| 潮玩 | 泡泡玛特 | 09992.HK | `r_hk09992` |
| 航运 | 中远海控 | 601919.SH | `sh601919` |

---

## 三、任务一：接行情源 + 安全边际尺

**方案**：`crawler/crawl.mjs` 每次运行时顺带请求腾讯免费行情接口（无需 key）：

```
https://qt.gtimg.cn/q=sh600519,sz000858,sz000568,r_hk00700,r_hk09992,sh601919,usPDD
```

**注意事项**：
- 返回是 **GBK 编码**（本地测试要 `iconv -f gbk -t utf-8`；Node 里用 `TextDecoder('gbk')` 解码）
- 返回格式：`v_sh600519="1~贵州茅台~600519~现价~昨收~今开~..."`，以 `~` 分隔，**第 4 个字段（下标 3）是现价**
- 港股代码前缀 `r_hk`，美股前缀 `us`；美股返回的价格是美元、港股是港币、A股是人民币——**币种要跟 targetPrice 一致才能算折价**（holdings 里 targetPrice 已按各自币种存）
- Actions 云端跑此接口应该畅通（腾讯接口无墙）；写入 `news.json` 的每个 company 加 `price`、`priceTime` 字段
- 失败容错：接口挂了不要让整个爬虫失败，price 置 null，前端显示「—」

**前端（总览页公司卡）**：
- 显示：现价 / 机构目标价 / 折价% = (targetPrice − price) ÷ targetPrice
- 折价 ≥30% → 绿色「击球区」徽章；0~30% → 中性；溢价 → 红色
- 首页 KPI 条加「击球区 N 只」卡（点击 `gotoView('dash')`）

### ✅ 完成情况（2026-07-24）
- 后端：`crawler/crawl.mjs` 新增 `fetchPrices()` 注入 `price/priceTime/priceCcy` 三个字段。接口 GBK 解码、容错置 null，7 只全部拉到。
- 前端：
  - `app.js` 新增 `parseTargetPrice()`（按数字附近 ±8 字符范围识别币种，避免「A 股 17 元 / 后接港币」被误判 HKD）。
  - `marginOf()` 拿 ≥30% → `hit` / 0~30% → `neutral` / 负数 → `premium`。
  - 总览公司卡：原「🎯 目标价」横带换成三联格（现价 / 目标价 / 折价），击球区标红加「击球区」徽章。
  - 首页 KPI 条加第 5 张卡「⚾ 击球区 N 只」，点击跳总览；≥1 只时高亮。
- **首次验证（2026-07-24 16:14）**：2 只击球区——
  - ⚾ 泸州老窖：现价 83.01 / 目标 128 CNY / 折价 +35.1%
  - ⚾ 腾讯控股：现价 434.6 / 目标 690 HKD / 折价 +37.0%
  - 中区（最接近）⚾：拼多多 折价 +29.2%，下一轮上涨击即可。
- 缓存版本：v10 → v11。

---

## 四、任务二：最新股息率 ⚠ 数据必须权威核实

**公式**：股息率 = 最近年度每股分红(税前 DPS) ÷ 现价，前端实时计算（现价来自任务一）。

**holdings.json 新增字段建议**：

```json
"dividendPerShare": 25.911,
"dividendYear": "2025年度",
"dividendCcy": "CNY",
"dividendSource": "公司2025年度权益分派实施公告"
```

**🚨 数据纪律（用户红线，绝对不能违反）**：
- **每股分红必须逐家核实**，来源用：neodata-financial-search 查询 / 公司年报及权益分派实施公告 / 巨潮资讯 / 港交所披露易。**禁止用记忆里的数字，禁止估算，禁止编造**
- 本文档**故意不写** 7 家的 DPS 数字——上一轮查询未完成交叉验证，接手者必须自己查（2025 年度分红，2026 年实施的那一期；若含中期+末期要合计）
- 拼多多（PDD）历史不分红，DPS = 0，股息率显示「不分红」
- 腾讯注意：股息为港币；另有实物分派历史（如美团、京东股份），只算现金股息
- A股注意「每10股派X元」口径要 ÷10 换算成每股；注意税前口径
- 写入时必须带 `dividendSource` 来源字段，前端展示来源标注（与披露日的做法一致）

---

## 五、任务三：买入检查清单

**holdings.json 新增字段建议**（每只一份，值为 true/false/null=待核）：

```json
"checklist": {
  "roe15": true,        // 近5年ROE≥15%（financials里已有数据可校验）
  "fcfPositive": true,  // 自由现金流为正
  "mgmtIntegrity": true,// 管理层诚信（一票否决项）
  "marginSafety": null, // 估值有安全边际（可由任务一自动判定：折价≥30%）
  "divOk": null         // 股息率达标（用户未给标准，先留手动位）
}
```

**前端**：总览页公司卡内可折叠区块（`<details>` 即可，别引库），✅/❌/⬜ 三态展示。`marginSafety` 可以由折价自动算，其余手动维护。清单初值请根据 financials 里已有的 ROE 数据如实判定 roe15，其余不确定的填 null 显示「待核」，**不要拍脑袋填 true**。

---

## 六、任务四：七力评分卡

**必须用用户定稿版中文命名**（不要用 Helmer 原书措辞）：
①规模经济 ②网络效应 ③反定位 ④转换成本 ⑤品牌效应 ⑥垄断性资源 ⑦流程优势

**holdings.json 新增字段建议**：

```json
"powers": [
  { "name": "品牌效应", "score": 5, "note": "心智垄断，超强定价权" },
  { "name": "垄断性资源", "score": 5, "note": "产区/工艺/基酒储备不可复制" }
]
```

- 多标签制：只列**确实成立**的力（1~3 个为常态），score 1~5
- 评分须有据可查：参考用户的研究框架，茅台=品牌+垄断性资源、腾讯=网络效应+转换成本这类结论可从其公开年报与商业常识论证，note 里写一句依据
- 前端：总览页公司卡里徽章化展示（力名 + 星级/分数），沿用板块色系统的视觉语言

---

## 七、全局注意事项（踩坑清单）

1. **仓库是公开的**：绝对不要写入持仓成本、股数、市值等隐私数据。目标价、评分、逻辑、DPS 都是公开信息，没问题。
2. **holdings.json 风格**：每条持仓一行的紧凑 JSON，用脚本改（参考 /tmp/patch_*.py 的做法），不要整个文件重排。
3. **news.json 很大（264KB+）**：patch 时只改 companies 段，别动新闻数组；用 `separators=(",", ":")` 保持紧凑。
4. **缓存版本号**：改了 `app.js`/`styles.css` 必须把 `index.html` 里的 `?v=10` 升到 `?v=11`，否则用户手机看不到更新。
5. **涨红跌绿**：中国市场惯例，折价/溢价、利好/利空的配色都遵循涨红跌绿（已有 CSS 变量 `--red` `--green`）。
6. **移动端优先**：所有新增区块必须在 720px 断点下可用（现有 media query 段照抄模式）。
7. **深浅色双主题**：新增样式要同时检查 `[data-theme=dark]` 下的可读性。
8. **改完必须**：`node --check app.js` 语法校验 → 本地 `python3 -m http.server 8765` 预览 → git commit + push。
9. **禁止 Mermaid / ASCII 图**（用户手机端看），文档一律用表格。
10. **爬虫是零依赖 Node**：不要引入任何 npm 包，fetch 用 Node 内置的。

---

## 八、近期已完成工作（供上下文）

- **2026-07-24**：财报披露日修复——7 家 2026 中报权威披露日已写入（茅台 08-15 / 五粮液 08-29 / 泸州老窖 08-26 / 腾讯 08-12 / PDD 08-24* / 泡泡玛特 08-19 / 中远海控 08-29，*PDD 为第三方预估待 IR 确认），crawler + 前端日历/总览均改为优先读权威日期。
- **2026-07-24**：板块修正（腾讯→互联网、拼多多→电商、泡泡玛特→潮玩）；新增 🏠 首页驾驶舱（KPI 条/持仓墙/财报倒计时/预警流/要闻/板块分布）。
- **2026-07-24**：全站 UI v10——深浅色主题切换、板块固定色系统、财报进度环、新闻卡片化、财务页 ROE 15% 生死线标色。
- **维护提醒**：本财报季过后需刷新 7 个披露日；PDD 官方确认后去掉「约」标记。

---

*文档生成：2026-07-24 21:33，WorkBuddy 会话交接用。*
